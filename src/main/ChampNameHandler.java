package main;

import javafx.event.ActionEvent;
import javafx.event.EventHandler;

/**
 * This is the handler for the text field. It controls what happens
 */
public class ChampNameHandler implements EventHandler<ActionEvent> {

    /**
     * method that gets called when enter key is hit. The input is
     * parsed and two Rectangle2D objects are created from it. Then the
     * classification is computed and set as the text for resultLabel.
     *
     * @param e The action event that triggers the handler
     */
    public void handle(ActionEvent e) {

    }
}
